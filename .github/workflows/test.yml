name: "test"

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: "Test"
    runs-on: "ubuntu-latest"

    steps:
      - name: "Checkout"
        uses: actions/checkout@v4

      - name: "Test"
        run: |
          go test -v ./...
